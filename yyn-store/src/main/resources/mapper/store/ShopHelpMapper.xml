<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yyn.store.mapper.ShopHelpMapper">
    
    <resultMap type="ShopHelp" id="ShopHelpResult">
        <result property="helpId"    column="help_id"    />
        <result property="title"    column="title"    />
        <result property="content"    column="content"    />
        <result property="sort"    column="sort"    />
        <result property="isDelete"    column="is_delete"    />
        <result property="storeId"    column="store_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectShopHelpVo">
        select help_id, title, content, sort, is_delete, store_id, create_by, create_time, update_by, update_time, remark from shop_help
    </sql>

    <select id="selectShopHelpList" parameterType="ShopHelp" resultMap="ShopHelpResult">
        <include refid="selectShopHelpVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="sort != null "> and sort = #{sort}</if>
            <if test="isDelete != null "> and is_delete = #{isDelete}</if>
            <if test="storeId != null "> and store_id = #{storeId}</if>
        </where>
    </select>
    
    <select id="selectShopHelpByHelpId" parameterType="Long" resultMap="ShopHelpResult">
        <include refid="selectShopHelpVo"/>
        where help_id = #{helpId}
    </select>
        
    <insert id="insertShopHelp" parameterType="ShopHelp" useGeneratedKeys="true" keyProperty="helpId">
        insert into shop_help
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">title,</if>
            <if test="content != null and content != ''">content,</if>
            <if test="sort != null">sort,</if>
            <if test="isDelete != null">is_delete,</if>
            <if test="storeId != null">store_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">#{title},</if>
            <if test="content != null and content != ''">#{content},</if>
            <if test="sort != null">#{sort},</if>
            <if test="isDelete != null">#{isDelete},</if>
            <if test="storeId != null">#{storeId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateShopHelp" parameterType="ShopHelp">
        update shop_help
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="content != null and content != ''">content = #{content},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="isDelete != null">is_delete = #{isDelete},</if>
            <if test="storeId != null">store_id = #{storeId},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where help_id = #{helpId}
    </update>

    <delete id="deleteShopHelpByHelpId" parameterType="Long">
        delete from shop_help where help_id = #{helpId}
    </delete>

    <delete id="deleteShopHelpByHelpIds" parameterType="String">
        delete from shop_help where help_id in 
        <foreach item="helpId" collection="array" open="(" separator="," close=")">
            #{helpId}
        </foreach>
    </delete>
</mapper>