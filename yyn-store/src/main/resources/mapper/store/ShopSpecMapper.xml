<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yyn.store.mapper.ShopSpecMapper">
    
    <resultMap type="ShopSpec" id="ShopSpecResult">
        <result property="specId"    column="spec_id"    />
        <result property="specName"    column="spec_name"    />
        <result property="storeId"    column="store_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectShopSpecVo">
        select spec_id, spec_name, store_id, create_by, create_time, update_by, update_time, remark from shop_spec
    </sql>

    <select id="selectShopSpecList" parameterType="ShopSpec" resultMap="ShopSpecResult">
        <include refid="selectShopSpecVo"/>
        <where>  
            <if test="specName != null  and specName != ''"> and spec_name like concat('%', #{specName}, '%')</if>
            <if test="storeId != null "> and store_id = #{storeId}</if>
        </where>
    </select>
    
    <select id="selectShopSpecBySpecId" parameterType="Long" resultMap="ShopSpecResult">
        <include refid="selectShopSpecVo"/>
        where spec_id = #{specId}
    </select>
    <select id="selectShopSpecBySpecName" resultMap="ShopSpecResult">
        <include refid="selectShopSpecVo"/>
        where spec_name = #{specName}
    </select>

    <insert id="insertShopSpec" parameterType="ShopSpec" useGeneratedKeys="true" keyProperty="specId">
        insert into shop_spec
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="specName != null and specName != ''">spec_name,</if>
            <if test="storeId != null">store_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="specName != null and specName != ''">#{specName},</if>
            <if test="storeId != null">#{storeId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateShopSpec" parameterType="ShopSpec">
        update shop_spec
        <trim prefix="SET" suffixOverrides=",">
            <if test="specName != null and specName != ''">spec_name = #{specName},</if>
            <if test="storeId != null">store_id = #{storeId},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where spec_id = #{specId}
    </update>

    <delete id="deleteShopSpecBySpecId" parameterType="Long">
        delete from shop_spec where spec_id = #{specId}
    </delete>

    <delete id="deleteShopSpecBySpecIds" parameterType="String">
        delete from shop_spec where spec_id in 
        <foreach item="specId" collection="array" open="(" separator="," close=")">
            #{specId}
        </foreach>
    </delete>
</mapper>